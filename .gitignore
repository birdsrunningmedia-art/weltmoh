# dependencies
/node_modules

# next.js
/.next/
/out/

# database (local operational file — never commit)
/data/

# env / secrets (sync key, Neon URL, session secret — never commit)
.env
.env*.local
.env*.production

# drizzle output (generated; keep migrations, ignore meta journals if noisy)
# NOTE: migrations are committed; nothing ignored here by default.

# OS / editor
.DS_Store
*.pem
